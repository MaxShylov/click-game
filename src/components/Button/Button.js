import React from 'react';
import PropTypes from 'prop-types';

// import styles from './Button.module.scss';

export const Button = () => {
  return (
    <div>
      Button
    </div>
  );
};

Button.propTypes = {};
